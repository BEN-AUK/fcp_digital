---
description: NZ Food Safety core development non-negotiables (Legal, Hardware, Auth, UI, Styling, Structure)
alwaysApply: true
---

# 核心开发底线 (STRICT NON-NEGOTIABLES)

## 1. 法律合规与数据真实性 (NZ Food Safety Integrity)
- **双时间戳**: 所有日志提交必须包含 `measured_at`（用户录入时间）和 `created_at`（服务器 NTP 时间）。若偏差 > 24h，标记 `is_backdated: true`。
- **强制纠正闭环**: 异常值（如冰箱 > 5°C）必须强制跳转至 `CorrectiveActionScreen`，未完成严禁退出。
- **存证水印**: 拍照组件必须在 Canvas 合成：`[设备ID] [实时NTP时间] [GPS坐标]`。
- **多语言**: 严禁硬编码。所有文案通过 `t('key')` 调用，报告强制映射至 `en-NZ`。

## 2. 硬件与数据采集逻辑 (AI + Manual Fallback)
- **设备识别**: 录入流程必须以扫码 (QR Identification) 为起点。
- **AI 提取**: 遵循 `Capture` → `AI OCR` → `Staff Confirmation`。置信度 < 0.6 必须高亮数字框。
- **摄像头与 GPS**: 强制后置摄像头；开启 `highAccuracy` GPS 监听。

## 3. 授权与安全架构 (Auth & Persistence)
- **双层隔离**: 严格区分 `Venue Context` (Firebase Auth) 与 `Staff Context` (本地持久化)。
- **持久化**: Firebase 必须配置 `browserLocalPersistence`。确保关机重启后餐厅授权状态不丢失。
- **写保护**: 所有 Firestore 写入必须携带 `venueId`。
- **Firestore 实体一致性准则**: 所有对 Firestore 的读写操作必须参考 `src/models/` 下对应集合的模型接口（`User`、`Venue`、`Invite`）。未来增加新字段时，必须先更新 `src/models/` 中对应模型文件，再修改业务代码。

## 4. 样式隔离与 UI 规范 (Styling & CSS Architecture)

### 4.1 单一数据源 (Single Source of Truth)
- **Theme 强制**: 颜色、间距、字号、圆角必须全部来自 `@/styles/theme`。严禁硬编码 hex 颜色或像素数值。

### 4.2 工业可用性 (Industrial Utility)
- **触控区域 (Touch Targets)**: 可交互元素最小高度/宽度 44px，优先使用 `theme.layout.touchableHeight`。
- **可读性 (Readability)**: 标签使用 `theme.typography.body`；字号不得低于 13px。

### 4.3 跨平台布局 (Cross-Platform Layout)
- **屏幕容器**: 所有屏幕必须使用（或被包裹于）在 Web 上 `maxWidth: 600` 且水平居中的容器。
- **样式写法**: 使用 `StyleSheet.create` 定义样式；仅在值为动态时使用内联样式。

### 4.4 视觉层级 (Visual Hierarchy)
- **主标题 (Main Titles)**: 居中、28px（title 风格）、加粗。
- **卡片/表面 (Cards / Surface)**: 使用 1px 细边框 `rgba(255,255,255,0.1)` 与背景区分。
- **空状态 (Empty States)**: 列表必须提供统一样式的「无数据」提示，保持界面专业一致。

### 4.5 其他
- **CSS 分离原则**:
  - **Tailwind 限制**: 单个元素严禁堆叠超过 6 个 Tailwind 类名。超过必须抽离至 CSS Modules。
  - **100dvh 适配**: 所有全屏页面必须使用 `height: 100dvh` 以解决移动端浏览器工具栏遮挡问题。
- **交互规范**: 核心按钮点击面积 ≥ 60x60px；严禁使用 Swipe 执行核心逻辑。

## 5. 项目结构与编码规范 (Project Structure - NEW)
- **组件文件夹模式 (Folder-Based Components)**:
  - **强制结构**: 严禁在 `src/components/` 或 `app/` 根目录下直接放置 `.tsx` 文件。
  - **专属目录**: 每个新组件必须创建自己的子文件夹。
    - 路径：`src/components/[ComponentName]/`
    - 必须包含：`index.tsx` (组件逻辑)
    - 必须包含：`[ComponentName].module.css` 或 `index.styles.ts` (专属样式)
- **App 路由禁止 index.tsx**: 禁止在 `app/` 目录下创建除根目录以外的 `index.tsx`；必须使用 `(folder)/folder.tsx` 的路由组模式保持文件名唯一性与可读性。
- **路由文件命名 (App Routes - (Folder)/File.tsx 模式)**:
  - **规则**: 所有页面必须以 `(folder)/folder.tsx` 形式存在；样式文件必须放在该同名路由组文件夹内，与页面同目录（即 `(folder)/folder.styles.ts`），禁止使用 `index.tsx` 堆叠。
  - **结构**: `app/(group)/([feature])/[feature].tsx` 与 `app/(group)/([feature])/[feature].styles.ts` 同目录（如 `(staff)/staff.tsx` + `(staff)/staff.styles.ts`、`(join)/join.tsx` + `(join)/join.styles.ts`、`(home)/home.tsx` + `(home)/home.styles.ts`）。
  - **样式引用**: 页面内通过 `./[feature].styles` 引用（与页面同目录）。
  - **特例**: 首屏使用 `(home)/home.tsx`，避免与路由根冲突。
- **类型安全**: 严格 TypeScript，严禁 `any`。所有 Log 必须符合 `src/types/logs.d.ts` 定义。
- **离线优先**: 使用 Zustand 管理本地状态，写入先落本地缓存，背景静默同步。
- **反馈闭环**: 必须提供 `isCapturing`、`isProcessingAI`、`isUploading` 的全局视觉反馈。