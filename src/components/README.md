# Components (文件夹模式)

每个组件必须拥有自己的子文件夹：

- 路径：`src/components/[ComponentName]/`
- 必须包含：`index.tsx`（逻辑）
- 必须包含：`[ComponentName].module.css` 或 `index.styles.ts`（样式）

示例：`src/components/Button/index.tsx` + `Button.module.css` 或 `Button.styles.ts`。
